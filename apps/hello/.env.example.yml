# -----------------------------------------------------------------------------
# Application configuration (example)
# Copy to .env and adjust values before deployment
# -----------------------------------------------------------------------------

APP_NAME=hello
IMAGE=nginxdemos/hello:plain-text
CONTAINER_PORT=8080
HOSTNAME=hello.test.example.com

# Optional
REPLICAS=1

# Image is injected by CI/CD

